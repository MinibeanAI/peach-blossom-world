import assert from 'node:assert/strict';import{createSceneNavigation}from'../navigation.js';const n=createSceneNavigation();for(const [x,z]of[[0,-4],[0,7],[3,7],[-8,6],[-8,-2]])assert(!n.house.valid(x,z),'building, table, chair and pond block walking');for(const target of[{x:4.4,z:7},{x:-4.8,z:2},{x:0,z:16.5}]){const start={x:0,z:13.5},p=n.house.path(start,target);assert(p.length);let last=start;for(const next of p){assert(n.house.clear(last,next));last=next}}assert(n.cave.path({x:7.5,z:-42},{x:9,z:-68}).length);assert(n.pond.path({x:-8,z:8},{x:-9,z:-7}).length);console.log('PASS courtyard and pastoral navigation');
